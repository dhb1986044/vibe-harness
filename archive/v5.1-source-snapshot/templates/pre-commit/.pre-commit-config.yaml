repos:
  - repo: local
    hooks:
      - id: memory-consistency
        name: Memory consistency check
        entry: python scripts/check_memory_consistency.py --strict
        language: system
        always_run: true
        pass_filenames: false
