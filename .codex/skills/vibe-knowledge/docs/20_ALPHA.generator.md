# α-生成器：Feature evolution (Plan-only)

1. Read `memory-bank/` first; align with PRD/architecture
2. Ask >=9 clarifying questions unless AC already explicit
3. Output a plan-only feature plan (steps + reuse + verification)
4. Update PRD if scope/AC changes
5. Doc-sync: update activeContext + append progress
