# Lessons 参考

本项目的经验教训库位于仓库根目录 `docs/LESSONS.md`。

默认消费协议：
- 先读 `Active Summary` 与 `索引`
- 默认只展开 `Pinned` + 最近 `5~10` 条 `活跃` 条目
- 命中标签 / 关键词 / 影响文件时，再按需展开正文或 `docs/LESSONS_ARCHIVE.md`

归档库位于 `docs/LESSONS_ARCHIVE.md`。
规则说明位于 `docs/LESSONS_RULES.md`。
