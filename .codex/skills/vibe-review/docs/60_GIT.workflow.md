# Git Workflow

- One commit = one logical change (maps to plan step or AC)
- Tests updated for behavior changes
- No secrets committed
- Doc-sync is mandatory (progress/activeContext; architecture if changed)
