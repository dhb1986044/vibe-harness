# 自定义技能与自动化（Skill Commands）

## 判断标准
- 每天做不止一次 → 值得封装成 skill
- 有明确输入输出 → 适合自动化

## vibecoding 扩展策略
- 用 skill-creator 生成新技能骨架
- 统一遵循：先澄清→plan-only→EXEC→落盘
- 把通用命令写入 `plans/commit-plan.md` 或 `docs/` 模板
