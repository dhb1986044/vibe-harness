# 自动修复 Bug（AI 调试助手）

## 输入最小集
- 错误日志/堆栈（原样）
- 复现步骤（1-3 步）
- 期望行为 vs 实际行为
- 运行环境（OS/版本/依赖）

## 工作流
1) 先定位（假设列表）→ 选择最可能的 1-2 个根因
2) 设计最小修复（不引入大重构）
3) 写测试或验证脚本（先能失败，再能通过）
4) 修复后：更新 `memory-bank/progress.md`；反模式 / 可复用的防范点 → 调用 vibe-lessons 写入 `docs/LESSONS.md`

## 安全线
- 不允许“修好了但没验证”
- 不允许“未授权大范围重构”
